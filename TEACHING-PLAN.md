# 8 月教学计划

> 课程编号 W1-1 / W1-2 / …，不标绝对日期。进度顺延，不追日历。
> 每周 6 天学习 + 1 天休息。每节课约 90 分钟。
> 锚点模板：📗学什么 · ✅过关标准 · 💡意义 · 🎤面试怎么考 · 📚关联八股文

---

## Week 1 · 代码走读 + 修假接口

**目标：** 读懂项目的每一段核心代码，顺手把假接口修成真的。

**闸门验收（W1-6）：**
- 随机抽 AuthController / JwtUtil / TokenStore / AiController / main.py / 前端数据流中的 3 段，讲清数据流（3 段全过）
- SQL 综合 10 道（从用户表、面试记录表出题，正确 8 道以上）
- 口述：一个用户从注册到面试结束，请求经过了哪些服务

---

### W1-1 · AuthController — 注册/登录/登出

📗 **学什么**
  讲清 /auth/register、/auth/login、/auth/logout 三个接口的完整流程：请求进来 → Controller 收到 → 调 Service → 写 DB → 生成 Token → 返回前端。

✅ **过关标准**
  不看书不看代码，口述三个接口的完整流程。漏步 ≤ 1 处。能说出每个参数从哪来、到哪去。

💡 **意义**
  不学：登录永远靠 setTimeout 模拟，后续路由守卫、JWT 验证、个人中心全做不了。
  学会：用户系统闭环，注册登录登出全走真接口。后端面试的第一道关就是认证流程。

🎤 **面试怎么考**
  "你项目里的登录是怎么实现的？"
  他要的关键词：数据库验证 → BCrypt 比对 → JWT 生成 → 前端存储 → 每次请求带 Header → 过滤器验证。六步串起来才算答对。

📚 **关联八股文**
  **Cookie vs Session vs Token：** Cookie 是浏览器自动带的篮子，Session 把数据存服务端，Token 把数据编码后放客户端。你的项目用 Token 是因为前后端分离 + 未来要 Docker 多容器部署，Session 要共享存储成本高。
  延伸搜索：掘金搜"Cookie Session Token 区别 一文读懂"

🔧 **动手：** 前端 Login.vue 的 `setTimeout` → `fetch('http://127.0.0.1:8080/auth/login')`

---

### W1-2 · JwtUtil + TokenStore — Token 生成/验证/存储

📗 **学什么**
  JWT 三段结构（Header/Payload/Signature）各存什么、怎么生成的。"验三道"的顺序和原因：验签名 → 验过期 → 查 TokenStore 黑名单。

✅ **过关标准**
  不看书，口述 JWT 三段结构 + 验三道流程。能解释"为什么验完签名还要查 TokenStore"。

💡 **意义**
  不学：Token 被伪造了你不知道，登出后旧 Token 还能用——安全是假的。
  学会：知道怎么防伪造、怎么让登出真正生效。这是后端安全最基础的东西。

🎤 **面试怎么考**
  "JWT 的三段结构分别是什么？为什么验完签名还要查 TokenStore？"
  第三道（查 TokenStore）是关键——JWT 本身无状态，登出后 Token 字符串还在有效期，必须额外存一份做黑名单。只答前两道，他会追问。

📚 **关联八股文**
  **JWT vs Session：** JWT 无状态、自带过期、可携带信息。Session 服务端存储、可随时踢人。微服务/多服务器用 JWT，单体/需强制踢人用 Session。JWT 缺点：无法主动失效——所以你才需要 TokenStore 做黑名单。
  延伸搜索：阮一峰《JSON Web Token 入门教程》、JavaGuide 认证授权章节

🔧 **动手：** 密码加 BCrypt——`BCryptPasswordEncoder` 替换明文存储

---

### W1-3 · AiController — Java 网关转发 Python

📗 **学什么**
  Java 后端不直接调 AI——前端调 `/ai/chat` → Java `RestTemplate` 转发到 `http://ai-service:8000/chat` → Python 处理 → 返回 Java → 返回前端。三个 AI 接口都走这条路。

✅ **过关标准**
  口述 AiController 的转发流程：从收到 HTTP 请求到返回给前端，经过哪些层、每层干了什么。能解释"为什么用 RestTemplate 而不是直接在前端调 Python"。

💡 **意义**
  不学：不知道微服务之间怎么通信，以后加新服务不知道怎么接。
  学会：知道网关模式——Java 是入口，Python 是 AI 能力提供者。这是微服务架构的基础模式。

🎤 **面试怎么考**
  "为什么 AI 服务用 Python 而不是直接写在 Java 里？两个服务怎么通信？"
  他要的关键词：Java 管业务/Python 管 AI → LangChain 生态在 Python → HTTP REST 通信 → 调试方便。如果你能说"LangChain 的 Java 版文档少、功能滞后"，面试官就知道你踩过坑。

📚 **关联八股文**
  **微服务通信方式：** HTTP REST 简单通用、gRPC 快适合内网高频、消息队列异步削峰。你的项目选 REST 因为 Java↔Python 跨语言最通用、请求频率低、调试方便。什么场景用 gRPC？微服务间每秒上千次调用、对延迟敏感。
  延伸搜索：掘金搜"微服务间通信方式对比 REST gRPC 消息队列"

🔧 **动手：** 面试结果存 DB——`/evaluate` 返回后，Mapper insert 到 interview_results 表

---

### W1-4 · main.py — 三条 AI 链路 + Prompt 设计

📗 **学什么**
  三条路由 `/generate-questions`、`/chat`、`/evaluate` 各自的 Prompt 模板、调 DeepSeek API、解析返回值。Prompt 怎么控制 AI 输出格式。

✅ **过关标准**
  不看代码，口述 `/generate-questions` 的完整链路：前端参数 → Java 转发 → Python 收到 → 拼接 Prompt → 调 DeepSeek → 解析返回 → 返回前端。能说出 Prompt 里为什么要有格式约束。

💡 **意义**
  不学：AI 返回格式乱，前端解析不了，用户看到的要么乱码要么空白。
  学会：Prompt Engineering 是 AI 应用开发的核心能力——不是调模型参数，是调提示词和输出解析。

🎤 **面试怎么考**
  "AI 生成的题目质量不稳定怎么办？怎么控制输出格式？"
  他要的关键词：Prompt 格式约束 + few-shot 示例 + 解析异常处理 + retry 兜底。说得出三层防御 = 真做过。

📚 **关联八股文**
  **Prompt Engineering 四要素：** ① 角色设定（告诉 AI 它是谁）② 任务描述（做什么）③ 格式约束（输出什么格式）④ 示例（few-shot，给 1-2 个例子让 AI 模仿）。缺①语气不对，缺③前端没法解析，缺④质量不稳定。
  延伸搜索：DeepSeek 官方文档的 Prompt 设计指南

🔧 **动手：** 前端评分面板动态化——InterviewSession.vue 的星星从 AI 返回值动态渲染

---

### W1-5 · 前端数据流 — Login → Home → Create → Session

📗 **学什么**
  Vue Router 的 query 参数传递：创建页填的公司名 → `router.push({path: '/interview/1', query: {...}})` → 面试页 `route.query.company` 读取。整个前端的数据怎么在页面间流动。

✅ **过关标准**
  口述从登录到面试结束，前端经过的所有页面 + 每个页面的数据从哪来、怎么传到下一页。能讲清 query 传参 vs Pinia 全局状态的适用场景。

💡 **意义**
  不学：加新页面不知道怎么传参数，参数丢了不知道去哪找。
  学会：整个前端的数据流在你脑子里是一条线，你在哪改、改什么——心里有数。

🎤 **面试怎么考**
  "面试创建页填的公司名，怎么传到面试对话页的？"
  他要的关键词：route query 传参 → 目标页 `route.query` 读取 → 数据量小所以不用 Pinia → 跨多层页面会用 Pinia。说出替代方案 = 知道什么时候用更好的方案。

📚 **关联八股文**
  **Vue 组件间传值四种方式：** ① props/emit 父子组件 ② route query/params 页面跳转 ③ provide/inject 祖先传后代 ④ Pinia 全局状态。你的项目：Create→Session 用 route query（✅ 正确），用户登录状态应该用 Pinia（还没做）。
  延伸搜索：Vue3 官方文档"状态管理"章节

🔧 **动手：** 侧边栏"管理员"→ 登录后显示真实用户名

---

### W1-6 · 闸门验收

1. 随机抽 3 段代码，你讲数据流 → 3 段全过
2. SQL 综合 10 道（用户表 + 面试记录表）→ 8 道以上 ✅
3. 口述全链路：从用户注册到面试评分结束，经过了哪些服务

**逐项验收，卡哪补哪，不卡的不重来。**

---

## Week 2 · RAG 知识库

**目标：** 实现文档上传→向量化→检索→拼 Prompt 出题的完整 RAG 链路，能讲清楚每一步原理。

**闸门验收（W2-6）：**
- 口述：一份 PDF 从上传到影响出题的全过程
- 走读 RAG 代码，随机抽一段你讲
- SQL 10 道（文档元数据表 + 面试记录表）

---

### W2-1 · RAG 原理

📗 **学什么**
  RAG = Retrieval Augmented Generation，先检索再生成。文档→分块→Embedding（转向量）→存向量库→用户提问时检索相关块→拼进 Prompt→AI 基于检索结果回答。

✅ **过关标准**
  口述 RAG 五个步骤，每步讲清"输入是什么、输出是什么、用什么工具"。能用大白话解释 Embedding 是什么。

💡 **意义**
  不学：AI 出题永远是通用题，跟用户上传的面经没关系。项目没有差异化。
  学会：你能让 AI 基于用户自己的材料出题——这是 AI 应用开发的标志性能力。

🎤 **面试怎么考**
  "RAG 是什么？为什么不直接把文档全部丢给 AI？"
  他要的关键词：Token 成本 > 上下文窗口 > 输出质量。能排出这三个原因的顺序说明你做过。

📚 **关联八股文**
  **RAG 四环节：** Ingest（文档读取+分块）→ Embed（转向量）→ Store（存向量库）→ Retrieve & Generate（检索+拼接+生成）。Embedding 是什么？把文本"翻译"成数字向量，语义相近的文本向量距离近。
  延伸搜索：LangChain 官方文档 RAG 章节

---

### W2-2 · 文档上传 + 分块 + Embedding

📗 **学什么**
  FastAPI 新路由 `/upload-document`，接收 PDF → `pypdf` 读文本 → `RecursiveCharacterTextSplitter` 分块 → 调 DeepSeek Embedding API 转向量 → 存 Chroma。

✅ **过关标准**
  口述从文件上传到向量入库的完整流程。能解释分块大小为什么选 500-1000，太大/太小的后果。

💡 **意义**
  不学：RAG 只存在于概念里，一行实现代码都写不出来。
  学会：掌握了 AI 应用开发最常见的工程链路——从用户上传到数据就绪。

🎤 **面试怎么考**
  "文档分块大小选多少？为什么？选大了或选小了有什么后果？"
  他要的关键词：500-1000 字符、太大检索不精确、太小语义不完整。不要求精确数字，但要求说出选大小的 tradeoff。

📚 **关联八股文**
  **分块策略：** 固定大小（最简单）、递归字符分割（你用的）、语义分块（最智能但最慢）。你的场景用递归字符分割：面经段落通常在 500-1000 字，按 `\n\n` 优先切。chunk_overlap 设 100-200 防止关键信息断在边界。
  延伸搜索：LangChain RecursiveCharacterTextSplitter 文档

---

### W2-3 · Chroma 存储 + 检索

📗 **学什么**
  `chromadb.Client()` → `collection.add(embeddings=..., documents=..., metadatas=...)` 存。检索时 `collection.query(query_embeddings=..., n_results=3)` 取最相关的 3 段。

✅ **过关标准**
  口述存和查的代码流程。能解释为什么选 Chroma 而不是 Milvus/Pinecone，为什么用余弦相似度。

💡 **意义**
  不学：向量库是 RAG 的存储引擎。不懂这一层，检索出问题你不会排查。
  学会：知道怎么做技术选型——轻量本地用 Chroma，海量生产用 Milvus，不想运维用 Pinecone 付费。

🎤 **面试怎么考**
  "为什么选 Chroma？向量检索用什么相似度？"
  他要的关键词：Chroma 轻量/本地/Python 原生 → 你的项目数据量小不需要 Milvus → 余弦相似度适合文本语义。选型有依据 = 工程师思维。

📚 **关联八股文**
  **向量相似度三种：** 余弦相似度（看方向，文本语义最常用）、欧氏距离（看绝对距离）、点积（看大小+方向）。文本 Embedding 用余弦相似度因为语义由向量的方向决定，向量长度不重要。
  延伸搜索：掘金搜"向量数据库选型 Chroma Milvus Pinecone"

---

### W2-4 · 拼 Prompt + 联调

📗 **学什么**
  改造 `/generate-questions`：接收文档参数 → 检索相关片段 → 拼进 Prompt 模板 → 调 AI 出题。关注 Token 管理——检索几段、每段多长、加上 JD 和模板后总共多少 Token。

✅ **过关标准**
  口述 Prompt 拼接逻辑：检索结果怎么塞进去、顺序是什么。能解释"Token 超了怎么办"的三种应对策略。

💡 **意义**
  不学：RAG 做完了但一调 AI 就报 token limit exceeded，功能断了不知道怎么修。
  学会：Token 管理是调 AI API 的基本功。知道上限在哪、超了怎么处理。

🎤 **面试怎么考**
  "如果把检索结果拼进 Prompt，Token 超了怎么办？"
  他要的关键词：截断检索结果、减少 n_results、换更大上下文窗口模型。知道三层应对 = 不是第一次碰到这个问题。

📚 **关联八股文**
  **Token 成本估算：** DeepSeek 输入约 ¥0.001/1K tokens。检索 3 段 × 800 字 ≈ 3000 tokens + JD + 模板 ≈ 4000-5000 tokens/次。按每天 10 次面试，一天几分钱。面试官问成本时你能算出来。
  延伸搜索：DeepSeek API 定价页面

---

### W2-5 · RAG 全链路 + 我埋错你找

📗 **学什么**
  全链路走通：上传 PDF → 分块 → 向量化 → 存 Chroma → 创建面试 → AI 出题引用文档内容 → 验证题目和文档内容相关。

✅ **过关标准**
  5 道埋错题至少找出 4 道，并说出正确的应该怎么写。

💡 **意义**
  代码能跑 ≠ 代码对的。L1→L2 级别的判对错练习——错误跨文件，模拟真实调试场景。

🎤 **面试怎么考**
  本节课不练面试题，练的是**判断力**——AI 写的代码你敢不敢直接提交？能不能看出问题？

📚 **5 道埋错题：**
  1. 分块大小设成 50（太小，语义断裂）→ L1
  2. Embedding API key 写错（调不通）→ L1
  3. 检索 `n_results=10`（Token 超了）→ L1
  4. Prompt 里检索结果没拼进去（RAG 形同虚设）→ L2
  5. Chroma collection 没持久化（重启数据丢了）→ L2

---

### W2-6 · 闸门验收

1. 口述：一份 PDF 从上传到影响出题的全过程
2. 走读 RAG 代码，随机抽一段你讲
3. SQL 10 道（文档元数据表 + 面试记录表）

---

## Week 3 · LangGraph Agent

**目标：** 把现有无状态 `/chat` 改造成有状态的 Agent，能判断追问/换题/结束。

**闸门验收（W3-6）：**
- 口述：State 怎么流转——从用户答完一句话到 Agent 决定下一步
- 走读 Agent 代码，随机抽一段你讲
- SQL 10 道（面试记录表综合查询）

---

### W3-1 · Agent 原理 — 状态机

📗 **学什么**
  现有 `/chat` 是无状态的——每次只传当前消息。Agent 化后定义一个 State（当前题号、问答历史、每道题表现），AI 根据 State 决定下一步——追问、换题、还是结束。

✅ **过关标准**
  口述 Agent 和普通对话的区别。能讲清 State 里有哪些字段、每个字段干什么用。

💡 **意义**
  不学：AI 没有"记忆"，不知道你是第几题、上一题答得好不好。面试体验像金鱼——三秒就忘。
  学会：Agent 有了上下文感知能力，面试流程自然、像真人在面。这是 AI 应用从"玩具"到"产品"的分水岭。

🎤 **面试怎么考**
  "你的面试 Agent 怎么知道什么时候该追问、什么时候该换题？"
  他要的关键词：Prompt 规则定义判断标准 + 代码兜底（最多追 3 轮）→ 两种机制配合才生产级。

📚 **关联八股文**
  **Agent 三种架构：** ① 简单链式（A→B→C，你的 /chat 现状）② 路由式（根据输入走不同分支）③ 状态机（有记忆，根据 State 决定下一步，你要做的）。从简单到复杂的演进顺序就是产品需求的演进顺序。
  延伸搜索：LangGraph 官方文档"State Management"

---

### W3-2 · LangGraph State 定义

📗 **学什么**
  新建 `ai-service/agent.py`，定义 `InterviewState(TypedDict)`：current_question、questions、history、follow_up_count、max_follow_ups。LangGraph 的 State Graph 怎么用。

✅ **过关标准**
  口述 State 的每个字段和它的作用。能解释"为什么用 LangGraph 而不是自己写 if-else"。

💡 **意义**
  不学：用 if-else 硬写分支逻辑，问题多了变面条，改不动。
  学会：用框架管理状态流转，改逻辑不用改结构。这叫"选对工具"。

🎤 **面试怎么考**
  "为什么用 LangGraph 而不是自己写 if-else？"
  他要的关键词：简单场景 if-else 够用 → 面试流程有多个分支（追问/换题/结束/评分）→ 状态多 if-else 变面条 → LangGraph 把流转画成图，每个节点干一件事。如果你能说"一开始我用 if-else 写的，分支多了就乱了"——面试官知道你是从坑里爬出来的。

📚 **关联八股文**
  **LangGraph vs LangChain：** LangChain 是链式（A→B→C），LangGraph 是图式（可循环、可分支）。你的 Agent 需要"追问→回答→再追问→换题"的循环——链式做不到，必须图式。
  延伸搜索：LangGraph 官方 Quick Start

---

### W3-3 · 追问决策 + 换题逻辑

📗 **学什么**
  Agent 节点 `should_follow_up(state)` → 调 AI 判断回答深度 → 返回 "follow_up" 或 "next_question"。追问最多 3 轮，超过自动换题（代码兜底）。

✅ **过关标准**
  口述 `should_follow_up` 的判断流程：Prompt 怎么写的 → AI 返回什么 → 代码怎么处理 → 兜底规则是什么。流程完整，漏步 ≤ 1 处。

💡 **意义**
  不学：Agent 要么一直追问不换题（用户困在第一题），要么不追问直接跳（体验像刷题机器人）。
  学会：Agent 有了判断力，面试流程自然。追问判断用短 Prompt 成本很低，关键是有兜底。

🎤 **面试怎么考**
  "追问判断用的还是 AI？为什么不用规则？成本会不会太高？"
  他要的关键词：语义判断规则做不到 → 确实用 AI → 短 Prompt 每次约 200 tokens 成本极低 → 加兜底（最多 3 轮）。如果你还能说出"真正贵的是出题和评分的大 Prompt"——说明你有成本意识。

📚 **关联八股文**
  **AI 调用的成本分层：** 追问判断用短 Prompt（~200 tokens，便宜）→ 出题用中 Prompt（~1000 tokens）→ 评分报告用长 Prompt（~3000 tokens，最贵）。做工程要知道钱花在哪。
  延伸搜索：DeepSeek API 定价 + Token 计算器

---

### W3-4 · 上下文管理

📗 **学什么**
  Agent 的 `history` 字段存对话历史。每次调 AI 取最近 N 轮拼进 Prompt。N 太大 Token 超、太小 AI 失忆。你的方案：滑动窗口（最近 10 轮），面试每道题独立不需要远古上下文。

✅ **过关标准**
  口述两种上下文管理策略（滑动窗口 vs 摘要）和各自的适用场景。能解释为什么面试场景用滑动窗口就够了。

💡 **意义**
  不学：聊久了 AI 开始胡说八道（上下文污染）或直接报错 token limit。多轮对话是 AI 应用的用户体验基线。
  学会：掌握了生产级 AI 应用的必备能力——长对话管理。

🎤 **面试怎么考**
  "如果面试进行了 30 轮对话，上下文窗口塞不下了怎么办？"
  他要的关键词：滑动窗口（最近 K 轮，面试场景够用）+ 摘要（AI 压缩旧对话，适合需长期记忆场景）。如果他追问"什么场景用摘要"→ 客服对话、长期陪伴，需要记住用户整体画像。

📚 **关联八股文**
  **上下文窗口三种策略：** ① 全量（最简单，Token 贵）② 滑动窗口（你的方案，折中）③ 摘要+滑动窗口（最复杂，适合长期对话）。面试用②就行——每道题独立，不需要记住前 3 题的细节。
  延伸搜索：掘金搜"大模型长对话上下文管理策略"

---

### W3-5 · Agent 全链路 + 我埋错你找

📗 **学什么**
  全链路走通：多轮多题面试，Agent 自动追问、自动换题、历史记录完整。L2→L3 级别的埋错——逻辑错误，语法对但行为错。

✅ **过关标准**
  5 道埋错题至少找出 4 道，并说出正确的应该怎么写。

📚 **5 道埋错题：**
  1. State 里 `current_question` 初始值没设（undefined）→ L2
  2. 追问计数 `follow_up_count` 用完了没重置（下一题直接跳过追问）→ L3
  3. 换题时 `history` 没清旧题对话（上下文混了，AI 把上道题的追问当成这道题的）→ L3
  4. `should_follow_up` 判断完没更新 State（Agent 永远卡在当前题）→ L3
  5. max_follow_ups 设为 10（一直追问不换题，第一题就劝退）→ L2

---

### W3-6 · 闸门验收

1. 口述：State 怎么流转——从用户答完一句话到 Agent 决定下一步
2. 走读 Agent 代码，随机抽一段你讲
3. SQL 10 道（面试记录表综合查询——JOIN + GROUP BY + HAVING + 窗口函数）

---

## Week 4 · 评分报告 + 综合收尾

**目标：** 实现结构化评分报告，综合验收全部学习指标。前 5 天学习+收尾，后 3 天综合闸门+博客+缓冲。

**闸门验收（W4-5~7）：**
- L3 判对错：10 道代码改错题，8 道以上 ✅
- SQL 终测：20 道综合题，16 道以上 ✅
- 架构默讲：5 分钟不卡壳 ✅
- 博客 1 篇发布 ✅

---

### W4-1 · Prompt Engineering — 结构化输出

📗 **学什么**
  改造 `/evaluate` 的 Prompt：从"请给面试评分" → 要求输出结构化 JSON（logic/depth/expression 各维度打分 + 亮点 + 改进建议）。三层防御：Prompt 示例 + 解析重试 + 降低 temperature。

✅ **过关标准**
  口述三层防御策略：每一层防什么问题、为什么需要三层而不是一层。能解释 temperature 改 0.3 和 0.7 的区别。

💡 **意义**
  不学：AI 返回的 JSON 解析失败，前端评分面板空白。
  学会：结构化输出是调 AI API 的日常操作。三层防御 = 你知道 AI 不可靠，做了工程兜底。

🎤 **面试怎么考**
  "AI 返回 JSON 格式不稳定怎么办？"
  他要的关键词：Prompt 里给 JSON 示例（few-shot）→ 代码 try/parse 失败重试 → 降低 temperature 让输出更确定。说得出三层防御 = 不是教科书选手。

📚 **关联八股文**
  **Temperature 的含义：** 0→1，越低输出越确定/保守，越高输出越有创意/随机。你的评分场景用 0.3——需要确定的结构化输出，不需要创意。写诗用 0.8，做数学用 0。
  延伸搜索：OpenAI API 文档 Temperature 参数说明

---

### W4-2 · 评分接口改造 + 前端动态化

📗 **学什么**
  Python `/evaluate` 返回结构化 JSON → Java 转发 → 前端 `InterviewSession.vue` 的评分面板从写死星星 → 动态渲染各维度分数。评分结果同时存 DB，历史页可查。

✅ **过关标准**
  口述评分数据的全链路：AI 生成 → Python 返回 → Java 转发 → 前端渲染 → DB 存储 → 历史页查询。链路完整，漏步 ≤ 1 处。

💡 **意义**
  不学：评分只是个数字，用户不知道为什么得这个分，历史页永远是假的。
  学会：掌握了"数据从生成到存储到展示"的全链路——这就是全栈。

🎤 **面试怎么考**
  "评分结果是怎么存到数据库的？用户查看历史时评分怎么展示？"
  面试官要听的就是全链路——生成→存储→展示。你能从头到尾讲清楚 = 全栈能力验证。

📚 **关联八股文**
  无新课。本节课练的是"全链路串联"，八股文在前面三周都覆盖了。

---

### W4-3 · 代码改错综合练习

📗 **学什么**
  10 道改错题，覆盖 Java（Controller/Service/Filter）、Python（FastAPI/Prompt）、Vue（数据流/组件）、SQL。L3 级别为主——逻辑错误，代码能跑但行为错。

✅ **过关标准**
  10 道至少找出并正确修改 8 道。

💡 **意义**
  前三周你练了"读懂代码"。本周你练的是"判断代码"——这是 vibecoding 时代最值钱的能力。

🎤 **面试怎么考**
  本课不练面试题。练的是：AI 生成的代码你敢不敢提交？这个判断力面试官不会直接考——但你代码里没隐藏 bug 就是证明。

📚 **10 道改错题范围：**
  Java：Service 层空指针没判断、JWT 过期时间异常没 catch、Filter 顺序错误
  Python：Prompt 缺少格式约束、异常没 retry、Chroma 查询结果为空时没兜底
  Vue：v-if/v-for 同时用、onMounted 里没 await、路由守卫 redirect 死循环
  SQL：WHERE vs HAVING 混用、JOIN 缺少 ON 条件、GROUP BY 漏字段

---

### W4-4 · 架构默讲 + 博客指导

📗 **学什么**
  不看任何资料，从用户打开浏览器开始，讲一个面试请求经过的所有服务——Vue → Nginx → Spring Boot → MySQL/Redis → Python → DeepSeek API → 返回。每层干什么、传什么数据、出错了怎么办。博客：1500 字技术总结，选 RAG 或 Agent 主题。

✅ **过关标准**
  架构默讲 5 分钟不卡壳，不跳层。博客发布到掘金，有开头/正文/代码示例/总结。

💡 **意义**
  架构默讲 = 面试官问"介绍下你的项目"时你能讲 5 分钟不重复。博客 = 证明你能把技术讲清楚——这是简历上最直接的面试邀请触发器。

🎤 **面试怎么考**
  "介绍一下你的项目吧。"——这是每场面试的开场白。
  你要在 3-5 分钟内讲清：做什么、用什么技术、架构怎么设计的、你遇到的最大挑战是什么。架构默讲就是练这个。

📚 **关联八股文**
  无。

---

### W4-5 · SQL 终测（上）

📗 **学什么**
  SQL 终测前 10 道。覆盖：JOIN（INNER/LEFT/RIGHT）、子查询（WHERE 子查询/FROM 子查询）、GROUP BY + HAVING、CASE WHEN、UNION/UNION ALL。

✅ **过关标准**
  10 道正确 8 道以上。

---

### W4-6 · SQL 终测（下）+ 全链路口述

📗 **学什么**
  SQL 终测后 10 道。覆盖：窗口函数（ROW_NUMBER/RANK）、多表联查（3 表以上）、复杂业务场景。然后全链路口述：项目 7 个服务怎么通信的。

✅ **过关标准**
  SQL 下半 10 道正确 8 道以上。总计 20 道正确 16 道以上。全链路口述不打结。

---

### W4-7 · 综合闸门 + 缓冲

📗 **学什么**
  四项逐个验收，卡哪补哪。全部通过后写 Week 4 周报。

✅ **四项验收：**
  1. L3 判对错：10 道代码改错题，8 道以上 ✅
  2. SQL 终测：20 道综合题，16 道以上 ✅
  3. 架构默讲：5 分钟不卡壳 ✅
  4. 博客 1 篇发布 ✅

---

## 综合学习目标总览

| 学习目标 | 验证方式 | 验收标准 |
|----------|----------|----------|
| L2 追流程 | 随机抽 3 段代码，讲数据流 | 3 段全对，漏步 ≤ 1/段 |
| L3 判对错 | 10 道代码改错题（L1→L3 梯度） | 8 道以上正确 |
| SQL 手写 | 20 道综合题 | 16 道以上正确 |
| 架构默讲 | 不看图讲清 7 个服务如何通信 | 5 分钟流利，不跳层 |
| RAG 原理 | 口述：文档从上传到影响出题全过程 | 五步完整，每步讲清输入/输出/工具 |
| Agent 状态管理 | 口述：State 怎么流转 + 追问决策流程 | 流程完整，漏步 ≤ 1 |

**以上 6 项，8/31 前全部过关。不过的项不放过，进度可延。**
